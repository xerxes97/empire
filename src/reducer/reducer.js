const initialState = {
    info:[],
    favorites:[]
}

const empire = (state = initialState, action)=>{
    switch(action.type){


        default:
            return state;
    }
}

export default empire;